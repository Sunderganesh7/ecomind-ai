# EcoMind AI Testing Suite

This directory contains the testing infrastructure for the Environmental Intelligence System.

## Structure
- `unit/`: Contains isolated tests for individual functions and classes.
- `integration/`: Contains tests that verify the interaction between multiple components (e.g., Database + API).
- `fixtures/`: Contains sample data and mocks used across tests.

## Running Tests
*(Instructions to be added once testing framework is configured)*
